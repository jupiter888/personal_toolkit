# you can specify the sheet, and use the read_excel function accordingly if the excel file has multiple sheets

df <- read_excel(file_path, sheet = "Sheet1",skip = 2, col_types = c("text", "text", "numeric","numeric","text"))

# can use col_select option of read_excel function to select specific columns

df <- read_excel(file_path, sheet = "Sheet1",col_select = c(2:6))
